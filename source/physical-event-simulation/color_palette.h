#pragma once

#include <SFML/Graphics/Color.hpp>

// Contient les constantes de la palette de couleur de l'application.
namespace color_palette {
	const sf::Color DARK_GRAY = { 30,102,102 };
	const sf::Color VERY_DARK_GRAY = { 22,44,44 };
	const sf::Color LIGHT_FOREGROUND = { 203,204,143 };
	const sf::Color LIGHT_TEXT = { 255,255,92 };
	const sf::Color DARK_TEXT = sf::Color::Black;
}